# Server
PORT=3001
NODE_ENV=development
JWT_SECRET=change-this-to-a-random-secret-min-32-chars
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Database (optional — defaults to ./server/pulse-crm.db in dev)
# DATABASE_PATH=/var/data/pulse-crm.db

# Anthropic AI (for reply classification + AI copywriter)
# ANTHROPIC_API_KEY=sk-ant-...

# SMTP (configure in Settings > Channels)
# WhatsApp Business API (configure in Settings > Channels)
# Credentials are stored in the database via channel_configs table
